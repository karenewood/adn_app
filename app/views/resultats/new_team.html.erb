<% provide(:title, 'Evaluation Fonctionnelle pour une equipe:') %>
<h1>Evaluation Fonctionnelle pour une equipe</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for @resultat do |f| %>
    <%= render 'shared/resultat_error_messages' %>

      <%= f.label :evaluation_id, "Evaluation" %>
      <div class="styled-select">
        <%= f.collection_select :evaluation_id, Evaluation.order(:name), :id, :name %>
      </div>

      <%= f.label :equipe_id, "Equipe" %>
      <div class="styled-select">
        <%= f.collection_select :equipe_id, Equipe.order(:name), :id, :name %>
      </div>

      <div class="submitButton">
        <%= f.submit "Creer", class: "btn btn-large btn-primary" %>
      </div>
    <% end %>
  </div>
</div>